.col-md-6
  h3 Top by Market Cap
  table.table.table-hover
    thead
      tr
        th Rank
        th Name
        th Market Cap (USD)
    tbody
      each crypto, index in cryptosSortedByMarketCap
        tr
          td= index + 1
          td= crypto.name
          td= `$${crypto.quote.USD.market_cap.toLocaleString()}`
