.col-md-6
  h3 Top by Price
  table.table.table-hover
    thead
      tr
        th Rank
        th Name
        th Price (USD)
    tbody
      each crypto, index in cryptosSortedByPrice
        tr
          td= index + 1
          td= crypto.name
          td= `$${crypto.quote.USD.price.toFixed(2)}`
